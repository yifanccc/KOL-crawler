[project]
name = "kol-collector-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["httpx>=0.27.0", "playwright>=1.48.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
